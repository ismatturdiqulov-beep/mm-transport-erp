-- MM Transport ERP — миграция 44: состояние двухшагового поиска в боте владельца
-- (2026-08-10). Кнопки "Контрагенты"/"ПС" в админ-меню бота просят прислать имя
-- следующим сообщением — вебхук без состояния между вызовами, поэтому "жду имя
-- контрагента" нужно где-то запомнить между двумя сообщениями. Храним прямо на
-- owner_notify (значения: null | 'kontragent' | 'fleet').
alter table owner_notify add column pending_query text;
