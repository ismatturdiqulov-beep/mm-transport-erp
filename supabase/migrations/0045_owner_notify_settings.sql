-- MM Transport ERP — миграция 45: переключатели личных Telegram-уведомлений
-- владельцу платформы (2026-08-10). Пользователь захотел иметь возможность
-- включать/выключать по категориям (ТИР/Дозвол, начисления контрагентов, касса)
-- без правки кода. Храним рядом с остальными company_settings-полями (тот же
-- паттерн, что tir_types/doz_prices/buy_prices и т.д.) — отдельная колонка,
-- не общий блоб, чтобы не путать с бизнес-настройками компании.
-- Уведомления о бэкапах (backup_log/db-backup) сюда не входят — они не завязаны
-- на company_settings и всегда включены, это инфраструктурный алерт, а не
-- бизнес-событие.
alter table company_settings add column owner_notify_settings jsonb;
