-- MM Transport ERP — миграция 49: адрес проживания у контрагентов (2026-08-12).
-- У водителей (people.address) поле уже было в базе — просто отсутствовал сам
-- input в форме (программный недосмотр, чинится без миграции). У контрагентов
-- поля не было вообще нигде — добавляем.
alter table kontragenty add column address text;
