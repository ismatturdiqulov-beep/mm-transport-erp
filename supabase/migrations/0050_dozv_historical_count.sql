-- MM Transport ERP — миграция 50: историческая корректировка счётчика Дозвол
-- (2026-08-29). Счётчик подтверждённых дозволов в личном кабинете Минтранса
-- считает ВСЕ подтверждённые заявки за всё время работы компании, а не только
-- с момента перехода на эту систему — старые дозволы (до системы) поштучно
-- восстановить нельзя, данные утеряны. Одно число-поправка на компанию:
-- прибавляется к count(dozv) для сверки с цифрой Минтранса, решение пользователя.
alter table company_settings add column dozv_historical integer not null default 0;
